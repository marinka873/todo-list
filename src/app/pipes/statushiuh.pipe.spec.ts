import { StatushiuhPipe } from './statushiuh.pipe';

describe('StatushiuhPipe', () => {
  it('create an instance', () => {
    const pipe = new StatushiuhPipe();
    expect(pipe).toBeTruthy();
  });
});
